import 'package:flutter/material.dart';

class ReturnsScreen extends StatelessWidget {
  static const String routeName = '/returns';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Returns'),
      ),
      body: Text('Beiginning of Returns Module'),
    );
  }
}
